import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { TypeFilterPage } from './type-filter';

@NgModule({
  declarations: [
    TypeFilterPage,
  ],
  imports: [
    IonicPageModule.forChild(TypeFilterPage),
  ],
})
export class TypeFilterPageModule {}
